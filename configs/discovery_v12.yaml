experiment_id: directional-range-conversion-asymmetry-v12
status: frozen_before_v12_bucket_c_evaluation
frozen_date: 2026-07-23
claim_type: exact conditional OHLC factor candidate; body/range measure itself is prior art

candidate_registry: [drca_60, drca_120]

formula:
  daily_body_efficiency: absolute(close - open) / (high - low)
  direction: sign of intraday log return minus eligible-universe median
  candidate: positive-day mean body efficiency minus negative-day mean body efficiency
  minimum_observations: ten positive and ten negative days
  horizons: [60, 120]
  expected_sign: positive

neutralization:
  common_controls: eleven known liquidity, reversal, volatility, lottery, momentum, and overnight/intraday baselines
  candidate_specific_controls:
    - intraday sign balance
    - unconditional mean body efficiency
    - unconditional mean close location within the daily range
    - linear intraday-return/body-efficiency correlation
  structural_controls:
    - z_log_float_market_cap
    - centered_squared_z_log_float_market_cap
    - point_in_time_Shenwan_L1_dummies

adaptive_reuse_disclosure: bucket C is adaptive research data; bucket D remains unopened

multiplicity:
  method: Benjamini-Hochberg
  prior_tests_included: 57
  current_tests: 2
  total_tests_after_v12: 59

periods:
  discovery: [2020-01-01, 2022-12-31]
  internal_validation: [2023-01-01, 2024-12-31]

research_gate:
  discovery_minimum_months: 34
  discovery_mean_rank_ic_min: 0.015
  discovery_bh_fdr_max: 0.10
  validation_minimum_months: 23
  validation_mean_rank_ic_min: 0.010
  validation_2023_and_2024_positive: true
  validation_mean_net_active_return_min: 0.0
  validation_net_information_ratio_min: 0.30

portfolio:
  type: buffered equal-weight long-only
  holdings: 20
  retention_percentile: 0.60
  cost_bps_one_way: 20

failure_rule: retain negative result and keep D closed if no candidate passes
